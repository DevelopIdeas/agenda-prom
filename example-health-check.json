{
  "alert_name": "pending_jobs_alert",
  "sql_query": "SELECT COUNT(*) as total FROM job_queue WHERE status = 'pending' AND created_at < DATE_SUB(NOW(), INTERVAL 1 HOUR)",
  "description": "Alert when jobs have been pending for more than 1 hour"
}